
             var arr=[];
             var root=[1,2,3];
            var i=root.length-1;
            while(i>=0)
            {
                if(root[i]!==null)
                {
                    arr.push(root[i]);
                }
                i--;
                
            }

            console.log(arr);
        </script>
        this not using linklist but perform with array.
