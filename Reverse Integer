/**
 * @param {number} x
 * @return {number}
 */
var x=123;
var reverse = function(x) {
    var rev=0;
    while(x>0)
    {
        rev=rev*10+x%10;
        x=Math.floor(x/10);
    }
    return rev;
};
